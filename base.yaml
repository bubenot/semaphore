- name: Install base packages on new servers Corporation of WNTN
  hosts: all
  tasks:
    - name: Install base packages
      block:
      - name: Install nginx
        apt:
          name: nginx
          state: latest
          update_cache: true
